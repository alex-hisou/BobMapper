<Window x:Class="BobMapper.View.MapPropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BobMapper.View" xmlns:viewmodel="clr-namespace:BobMapper.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewmodel:MapPropertiesWindowViewModel}"
        mc:Ignorable="d"
        Title="Map Properties" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="23*"/>
            <RowDefinition Height="48*"/>
            <RowDefinition Height="52*"/>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="52*"/>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="52*"/>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="52*"/>
            <RowDefinition Height="43*"/>
            <RowDefinition Height="157*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64*"/>
            <ColumnDefinition Width="232*"/>
            <ColumnDefinition Width="198*"/>
            <ColumnDefinition Width="199*"/>
            <ColumnDefinition Width="64*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Name" FontSize="20" Grid.Column="1" Grid.Row="1"/>
        <TextBox Text="{Binding CurrentMapProperties.Name, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="3" FontSize="20"/>
        <CheckBox Content="Apartment level" IsChecked="{Binding CurrentMapProperties.IsApartment}" Grid.Column="1" Grid.Row="4">
            <CheckBox.RenderTransform>
                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
            </CheckBox.RenderTransform>
        </CheckBox>
        <CheckBox Content="Night level" IsChecked="{Binding CurrentMapProperties.IsNightTime}" Grid.Column="1" Grid.Row="6">
            <CheckBox.RenderTransform>
                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
            </CheckBox.RenderTransform>
        </CheckBox>
        <Label Content="Tileset" FontSize="20" Grid.Column="2" Grid.Row="6"/>
        <ComboBox ItemsSource="{Binding TilesetEnum}" SelectedItem="{Binding CurrentMapProperties.Tileset}" Grid.Column="3" Grid.Row="6">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding ChangeTilesetCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ComboBox>
        <Label Content="Apartment height" Grid.Row="4" Grid.Column="2" FontSize="20"/>
        <TextBox Grid.Row="4" Grid.Column="3" IsEnabled="{Binding CurrentMapProperties.IsApartment}" Text="{Binding CurrentMapProperties.ApartmentHeight, UpdateSourceTrigger=LostFocus}" 
                    PreviewTextInput="TextBox_PreviewTextInput"
                    PreviewKeyDown="TextBox_PreviewKeyDown"/>
        <Label Content="Background" Grid.Row="8" Grid.Column="1" FontSize="20"/>
        <ComboBox ItemsSource="{Binding Backgrounds}" SelectedItem="{Binding CurrentMapProperties.BackgroundImage}" Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="8"/>
    </Grid>
</Window>

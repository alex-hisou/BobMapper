<Window x:Class="BobMapper.View.Prefrences"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BobMapper.View" xmlns:viewmodel="clr-namespace:BobMapper.ViewModel" 
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        d:DataContext="{d:DesignInstance Type=viewmodel:PrefrencesViewModel}"
        mc:Ignorable="d"
        
        Title="Prefrences" Height="201" Width="517">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="34*"/>
            <RowDefinition Height="36*"/>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="36*"/>
            <RowDefinition Height="19*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="327*"/>
            <ColumnDefinition Width="26*"/>
            <ColumnDefinition Width="83*"/>
            <ColumnDefinition Width="31*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Steam resources.dat file" Grid.Column="1" Margin="0,20,0,0" Grid.RowSpan="2"/>
        <TextBox Text="{Binding SteamPath}" IsReadOnly="True" Grid.Column="1" Grid.Row="2">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="TextChanged">
                    <i:InvokeCommandAction Command="{Binding SettingChangedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </TextBox>
        <Button Content="Change" Grid.Column="3" Grid.Row="2" Command="{Binding ChangeSteamDirCommand}"/>
        <CheckBox Content="Auto-select newly-created objects" IsChecked="{Binding AutoSelect}" Grid.Column="1" Grid.Row="4">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Checked">
                    <i:InvokeCommandAction Command="{Binding SettingChangedCommand}"/>
                </i:EventTrigger>
                <i:EventTrigger EventName="Unchecked">
                    <i:InvokeCommandAction Command="{Binding SettingChangedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </CheckBox>
        <Button Content="Apply" Grid.Column="3" Grid.Row="4" Command="{Binding ApplyCommand}" IsEnabled="{Binding NewChanges}"/>
    </Grid>
</Window>
